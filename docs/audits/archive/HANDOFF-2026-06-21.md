# Handoff — HH extraction session, 2026-06-21

## TL;DR

Signal-gated stub re-extraction. Proved the **literal-"happy hour" bucket converts (~44%, 9 new live venues)**; the broad **deal_or_menu bucket does not (~1 venue / $0.63)**. Next step = work the 86-venue literal-HH list across the un-mined cities. Session spend: **$1.52**.

## Merged to main (done)

- **#202** `fix/lazy-render-interop` — headless render was silently disabled in ALL CLI extraction (tsx dynamic-import returns named exports under `.default`). Render now works in scripts.
- **#203** `diag/stub-signal-scan` — new `pnpm scan:stub-signal --city X --state Y` ($0): buckets stubs into `happy_hour` / `deal_or_menu` / `no_signal` / `social_only` / `unreachable`.
- **#204** `chore/remove-pho-ever-tacoma` — Pho Ever (parked domain, ParkLogic) soft-deleted + blocklisted.

## Local-only, needs operator publish to prod (9 new live venues)

- **SLO (3):** Central Coast Brewing · Central Coast Brewing | Higuera *(bare)* · Hoagies (SLO)
- **Santa Barbara (6):** Azul Cocina ⭐(10 offerings) · Bettina · Little Alex's · Moby Dick · Paloma *(bare)* · The Harbor *(bare)*
- "bare" = window captured, no offerings → run `reextract:stubs --city X --state Y --bare` to heal.

## DECISIONS (locked)

1. **Work the literal-HH bucket; skip broad deal_or_menu.** deal_or_menu is recall-first noise (operating hours / food specials); proven poor ROI.
2. **Don't add new cities yet** — mine the existing 12 first. Discovery+enrich already sunk; new city = ~$35 enrich for unproven yield.
3. **Biggest opportunity = AZ** (Tucson 17 literal-HH, Scottsdale 19, Phoenix 11), all un-mined.

## NEXT ACTION

Review `docs/high-confidence-hh-candidates-2026-06-21.md` (86 literal-HH candidates, 9 un-mined cities, chain-filtered attempt, with on-page snippets). If it holds up, extract the set (render now fixed). **Gate it on operator $-OK.** Est ~$1.30 batch / ~$2.58 sync for 86.

**Skip on sight:** Applebee's, Hooters (chains the denylist missed — see bug below); junk-TLD sites (`voltd.top`, `.cfd`, `.shop`) — likely parked/hijacked like Pho Ever, verify first.

## OPEN ISSUES / GOTCHAS

- **Chain denylist BUG:** `isDenylistedChain("Hooters")` and `("Applebee's Grill + Bar")` return *keep* despite being in `CHAINS`. Chains leak into candidates + discovery. Worth a fix in `lib/places/chainDenylist.ts`.
- **Batch path flaky:** `reextract:stubs` default (BATCH) printed `[BATCH] N stubs` then exited without persisting some results this session. Use `--quick` (sync) or `--venue <id> --url <site>` (proven) until the batch submit/poll path is debugged.
- **Reconcile churn:** re-extracts created duplicate windows (Moby Dick ×2, CCB Higuera ×2). Run `pnpm reconcile:windows --city X --state Y --apply` per live city to collapse.
- **Scan JSONs:** `docs/*-stub-signal-2026-06-21.json` exist for the 9 sweep cities (AZ×3, WA×2, CA: oakland/san-mateo/berkeley/daly-city). The 3 CA Central Coast cities' JSON were deleted; their literal-HH were already worked in run 1.

## TOOLS ADDED

- `scripts/db-query.ts` — `tsx scripts/db-query.ts "SELECT …"` runs read-only SQL with **no permission prompt** (allowlisted, write-guarded). Use for all ad-hoc reads.
- `pnpm scan:stub-signal --city X --state Y` — $0 pre-spend signal report.

## CONSTRAINTS

- Operator handles ALL prod deploys (publish the 9 new venues).
- Per-run $-OK required before any paid extraction.
